// Сохранение данных

var todos = ['item 1', 'item 2', 'item 3'];

// Вывод данных

console.log(todos);

// Добавление новых данных

todos.push('item 4');
console.log(todos);

// Изменение данных 

todos[0] = 'start';
console.log(todos);

// Удаление данных

todos.splice(0, 1);
console.log(todos);
